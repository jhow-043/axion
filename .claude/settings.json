{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys; f=sys.argv[1]; f.endswith('.py') and __import__('subprocess').run(['ruff','check','--fix',f], check=False) or None\" \"$CLAUDE_FILE_PATH\"",
            "description": "Roda ruff check --fix em arquivos Python após edição"
          },
          {
            "type": "command",
            "command": "python -c \"import sys; f=sys.argv[1]; f.endswith('.py') and __import__('subprocess').run(['ruff','format',f], check=False) or None\" \"$CLAUDE_FILE_PATH\"",
            "description": "Roda ruff format em arquivos Python após edição"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"[hook] Bash autorizado\"",
            "description": "Placeholder para auditoria de comandos bash"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(ruff:*)",
      "Bash(pytest:*)",
      "Bash(alembic:*)",
      "Bash(uvicorn:*)",
      "Bash(docker:*)",
      "Bash(git status)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(git branch*)"
    ]
  }
}
